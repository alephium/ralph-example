Contract TestToken(
    symbol: ByteVec,
    name: ByteVec,
    decimals: U256,
    totalSupply: U256,
    owner: Address
) {
    pub fn transfer(to: Address, amount: U256) -> () {
        transferToken!(callerAddress!(), to, selfTokenId!(), amount)
    }
} 