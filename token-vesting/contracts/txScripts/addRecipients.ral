TxScript AddRecipients(vestingContract: TokenVesting, addresses: [Address; 100], amounts: [U256; 100], totalAmount: U256 ) {
  vestingContract.addRecipients{callerAddress!() -> ALPH: totalAmount + 100 alph}(addresses, amounts, totalAmount)
}